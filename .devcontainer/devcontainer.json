{
    "name": "LocalStack Setup",
    "image": "python:3.8",
    "settings": { 
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "forwardPorts": [4566],
    "postCreateCommand": "pip install localstack && localstack start && apt-get update && apt-get install ./docker-desktop.deb && systemctl --user start docker-desktop",
    "extensions": [
        "ms-python.python"
    ],
    "remoteUser": "root"
}
